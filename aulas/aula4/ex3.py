distancia = int(input("Digite a distância da viagem (quilometros): "))
vel_media = float(input ("Digite a velocidade média (km/h): "))

tempo = distancia / vel_media

print ("O tempo gasto nessa vigem foi de " + str(int(tempo)) + " horas")